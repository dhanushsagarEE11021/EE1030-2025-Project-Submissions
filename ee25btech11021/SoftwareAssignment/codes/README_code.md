# HOW TO EXECUTE THE CODE
since i used only c code ,these are coding only to run c codes

these codes should be runned onced the codes are written in svd.c file
## codes 
- ```gcc svd.c -o svd -lm -Iinclude```
- ```./svd```
## How to install clang
- ```pkg update && pkg upgrade```
- ```pkg install clang```
- ```pkg install build-essential```
